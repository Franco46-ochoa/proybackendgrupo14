"use strict";

// No-op: empresaId (antes duenoId) ya está incluido en la migración create-usuarios
module.exports = {
  async up(queryInterface, Sequelize) {},
  async down(queryInterface, Sequelize) {},
};
